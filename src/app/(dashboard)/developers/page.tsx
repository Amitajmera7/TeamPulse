import { StubPage } from "@/components/common/stub-page";

export default function DevelopersPage() {
  return (
    <StubPage
      header={{
        title: "Developers",
        description: "Individual performance, workload, and delivery history",
      }}
      sectionLabel="Developer Analytics"
      sectionDescription="Profiles, trends, and estimate accuracy will appear here"
    />
  );
}
