/**
 * TeamPulse — Allocation Module data barrel.
 *
 * Sprint 2 exports a mock `AllocationReadModel` only. Sprint 3 replaces the
 * mock modules with the real fetch → normalize → read-model pipeline and
 * exports the same `AllocationReadModel` type from here, so no UI import
 * has to change.
 */
export { ALLOCATION_WORKING_DAY_HOURS, MOCK_ALLOCATION_READ_MODEL } from "./mock-allocation-model";
